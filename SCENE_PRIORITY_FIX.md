# 场景优先级冲突修复

## 问题描述

在注释场景中，用户期望切换为中文输入法来写注释。但是当输入了几个英文字母后，自定义规则"连续英文字母切换为英文"会被触发，导致输入法又切换回英文，干扰了中文输入。

## 问题原因

1. **自定义规则优先级最高**：在 `EditorEventListener` 中，自定义规则的检查在最前面，优先级高于基础场景（注释、字符串、代码）
2. **默认规则应用范围过广**：以下两个默认规则在注释区域也生效：
   - "英文字母之间切换为英文"（`applyToCommentArea = true`）
   - "连续英文字母切换为英文"（`applyToCommentArea = true`）
3. **场景冲突**：
   - 注释场景配置：切换为中文
   - 自定义规则：检测到英文字母，切换为英文
   - 结果：用户无法在注释中正常输入中文

## 解决方案

修改默认自定义规则，将注释区域排除在外：

```kotlin
// 修改前
CustomPatternRule(
    name = "英文字母之间切换为英文",
    applyToStringArea = true,
    applyToCommentArea = true,  // ❌ 会干扰注释场景
    applyToCodeArea = false,
    ...
)

// 修改后
CustomPatternRule(
    name = "英文字母之间切换为英文",
    applyToStringArea = true,
    applyToCommentArea = false,  // ✅ 不应用于注释区域
    applyToCodeArea = false,
    ...
)
```

## 修改的规则

### 1. "英文字母之间切换为英文"
- **修改前**：应用于字符串和注释区域
- **修改后**：仅应用于字符串区域
- **原因**：注释区域通常用于写中文说明，不应该因为输入英文字母就切换回英文

### 2. "连续英文字母切换为英文"
- **修改前**：应用于字符串和注释区域
- **修改后**：仅应用于字符串区域
- **原因**：同上

## 场景优先级说明

当前的场景优先级（从高到低）：

1. **自定义规则场景**（最高优先级）
   - 通过正则表达式匹配光标左右文本
   - 可以针对特定文件类型和编辑区域
   
2. **字符串场景**
   - 有独立的状态管理
   - 支持习惯记录
   
3. **注释场景**
   - 由 `commentMethod` 配置控制
   
4. **代码场景**
   - 由文件类型规则或 `defaultMethod` 控制

## 使用建议

### 如果你需要在注释中输入英文代码

如果你经常在注释中写英文代码示例，可以：

1. **临时禁用规则**：在设置中禁用"连续英文字母切换为英文"规则
2. **手动切换**：使用系统快捷键（如 Control+Space）手动切换输入法
3. **自定义规则**：创建新的自定义规则，针对特定模式切换为英文

### 如果你需要在注释中混合中英文

当前的配置已经很适合这种场景：
- 进入注释区域：自动切换为中文
- 输入中文：保持中文输入法
- 输入英文：可以直接输入（中文输入法也能输入英文）
- 输入拼音：不会被自动切换为英文

### 自定义你的规则

你可以在"自定义规则场景"中创建自己的规则，例如：

**示例1：在注释中，如果左侧是代码符号，切换为英文**
```
名称：注释中代码符号后切换英文
左侧模式：.*[(){}\[\];,.]$
右侧模式：（空）
应用区域：仅注释区域
目标输入法：英文
```

**示例2：在注释中，如果左侧是中文，保持中文**
```
名称：注释中中文后保持中文
左侧模式：.*[\u4e00-\u9fa5]$
右侧模式：（空）
应用区域：仅注释区域
目标输入法：中文
```

## 对现有用户的影响

### 新安装用户
- 默认规则已经修改，不会遇到此问题

### 已安装用户
- **已有的自定义规则不会被修改**（保留用户配置）
- 如果遇到此问题，需要手动修改：
  1. 打开设置：Settings → FreeMacInput → 自定义规则场景
  2. 找到"英文字母之间切换为英文"规则
  3. 取消勾选"应用于注释区域"
  4. 找到"连续英文字母切换为英文"规则
  5. 取消勾选"应用于注释区域"

## 测试场景

修复后，以下场景应该正常工作：

### 场景1：注释中写中文
```java
// 这是一个测试方法
public void test() {
    // 输入中文时不会被切换为英文 ✅
}
```

### 场景2：注释中混合中英文
```java
// 这个方法用于处理 HTTP 请求
// 当 status code 为 200 时表示成功 ✅
```

### 场景3：字符串中输入英文
```java
String name = "hello world";  // 字符串中输入英文会自动切换 ✅
```

### 场景4：代码区域
```java
public void testMethod() {  // 代码区域保持英文 ✅
    int count = 0;
}
```

## 总结

这次修复确保了：
- ✅ 注释场景由"注释场景配置"完全控制
- ✅ 自定义规则不会干扰注释场景的基本行为
- ✅ 字符串场景仍然可以使用自定义规则
- ✅ 用户可以根据需要自定义规则
