pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven("https://cache-redirector.jetbrains.com/plugins.gradle.org/m2")
        maven("https://maven.aliyun.com/repository/jetbrains/")
    }
}

rootProject.name = "FreeMacInput"
